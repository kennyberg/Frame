var id = "<%= @photo.api_id %>"
// we retrieve the id (from the api) of the photo, and store it.

//we select the heart icon, and change the color on clic, without refreshing the page.
document.querySelector(`.fa-heart[data-id='${id}']`).style.color = "red"

