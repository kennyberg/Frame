<h1>Photos#show</h1>
<p>Find me in app/views/photos/show.html.erb</p>

<% if @photo.cl_url %>
  <%= cl_image_tag @photo.cl_url, height: 300, width: 400, crop: :fill %>
<% else %>
  <img src="<%= @photo.api_url %>" height="300" width="400" alt="test">
<% end %>
