<%= simple_form_for photo do |f| %>
  <!-- [...] -->
  <%= f.input :title %>
  <%= f.input :uploaded_url %>
  <%= f.input :uploaded_url_cache, as: :hidden %>
  <%= f.button :submit %>
  <!-- [...] -->
<% end %>
